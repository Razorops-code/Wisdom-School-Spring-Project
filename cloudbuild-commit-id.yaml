substitutions:
  _REPO: "asia-docker.pkg.dev/$_PROJECT_ID/school/sch"

steps:
  # Step 1: Check out the repository (Cloud Build clones the source automatically)
  - name: gcr.io/cloud-builders/git
    entrypoint: "bash"
    args:
      - "-c"
      - |
        echo "Source code is already cloned by Cloud Build."
        git checkout $(git rev-parse --short=6 HEAD)  # Explicitly check out the commit

  # Step 2: Set up JDK (for Maven)
  - name: gcr.io/cloud-builders/mvn
    args:
      - clean
      - package

  # Step 3: Authenticate to Google Cloud
  - name: gcr.io/cloud-builders/gcloud
    args:
      - auth
      - configure-docker
      - asia-docker.pkg.dev

  # Step 4: Get the first 6 characters of the commit SHA
  - name: gcr.io/cloud-builders/git
    entrypoint: "bash"
    args:
      - "-c"
      - |
        COMMIT_SHA=$(git rev-parse --short=6 HEAD)
        echo "Commit SHA (first 6 chars): $COMMIT_SHA" > /workspace/commit_id.txt

  # Step 5: Build the Docker image and tag it with the commit SHA
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - "-t"
      - "$_REPO:$COMMIT_SHA"
      - "."

  # Step 6: Push the Docker image with the commit SHA tag
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - "$_REPO:$COMMIT_SHA"

logsBucket: gs://school-build-logs

options:
  logging: GCS_ONLY
